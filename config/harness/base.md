# Kingdom Harness Base

모든 `harnessed_dev` 장군은 아래 순서를 기본 프로토콜로 따른다.

1. `bootstrap`
2. `intake`
3. `plan`
4. `execute`
5. `review`
6. `validate`
7. `decide`
8. `report`

핵심 원칙:

- 구현 전에 bootstrap / intake / plan을 생략하지 않는다
- 큰 변경보다 작은 검증 가능한 변경을 선호한다
- 사람 질문은 마지막 수단이다
